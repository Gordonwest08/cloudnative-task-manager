apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: production
data:
  PORT:          "5000"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB:   "taskmanager"
  CORS_ORIGIN:   "*"
